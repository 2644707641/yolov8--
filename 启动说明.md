# 启动脚本使用说明

## 快速启动

### Windows PowerShell 用户（推荐）

```powershell
# 进入项目目录
cd "d:\代码库\应用开发\yolov8预测停车位"

# 运行 PowerShell 启动脚本
.\start.ps1
```

### Windows CMD 用户

```cmd
# 进入项目目录
cd /d "d:\代码库\应用开发\yolov8预测停车位"

# 双击或运行批处理文件
start.bat
```

或者直接在文件资源管理器中**双击 `start.bat`**

## 脚本功能

两个启动脚本都会自动执行以下操作：

1. ✅ 检查环境变量文件（`.env` 和 `backend\.env`）
2. ✅ 检查并自动找到 Python 虚拟环境（`.venv` 或 `venv` 或 `backend\venv`）
3. ✅ 启动后端服务（Python FastAPI）
4. ✅ 启动前端服务（Vite + React）

## 启动后访问地址

- **前端界面**: http://localhost:3000
- **后端 API**: http://localhost:8000
- **API 文档**: http://localhost:8000/docs

## 常见问题

### PowerShell 执行策略错误

如果运行 `start.ps1` 时提示无法执行脚本，运行以下命令：

```powershell
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
```

### 端口被占用

如果端口 3000 或 8000 被占用，请先关闭占用端口的程序：

```powershell
# 查看端口占用
netstat -ano | findstr :3000
netstat -ano | findstr :8000

# 根据 PID 终止进程
taskkill /PID <进程ID> /F
```

### 虚拟环境未找到

如果提示未找到虚拟环境，请先创建并安装依赖：

```powershell
# 创建虚拟环境
python -m venv .venv

# 激活虚拟环境
.\.venv\Scripts\Activate.ps1

# 安装后端依赖
pip install -r backend\requirements.txt

# 安装前端依赖
npm install
```

## 手动启动（高级用户）

如果需要手动控制启动过程：

### 后端

```powershell
# 激活虚拟环境
.\.venv\Scripts\Activate.ps1

# 进入后端目录
cd backend

# 启动后端
python main.py
```

### 前端

```powershell
# 在项目根目录
npm run dev
```

## 其他脚本

- **`setup.bat`** - 首次安装所有依赖
- **`restart_backend.bat`** - 单独重启后端服务
