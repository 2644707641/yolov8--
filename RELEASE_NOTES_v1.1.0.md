# 🎉 YOLOv8 智能识别停车位系统 v1.1.0 发布说明

**发布日期**: 2025-10-18  
**版本**: 1.1.0  
**类型**: 功能更新 + Bug修复

---

## 📋 更新概览

本次更新主要聚焦于**用户体验优化**和**工作流程改进**，新增了重新开始功能，修复了文件输入框的bug，并优化了检测参数的默认值。

---

## ✨ 新增功能

### 1️⃣ 重新开始按钮

在识别结果页面（步骤4）底部新增了**"重新开始"**按钮，用户可以一键重置所有状态，开始新的识别任务。

**功能特点**：
- 🎯 **一键重置** - 清空所有当前识别数据和设置
- 🔄 **自动返回** - 重置后自动跳转到步骤1
- 🛡️ **安全确认** - 弹出确认对话框，防止误操作
- 🎨 **精美设计** - 绿色渐变按钮，带悬浮动画效果
- 📦 **保留历史** - 不会删除历史记录中的数据

**清空的内容**：
- 已上传的模型文件
- 已选择的图片/视频文件
- 当前的识别结果
- 检测参数（重置为默认值）
- 左侧导航的完成状态标记

**保留的内容**：
- 历史记录中的所有检测记录
- 云端存储的原始文件和结果文件
- 用户登录状态

**使用场景**：
```
完成一次识别 → 查看结果 → 点击"重新开始"
→ 清空界面 → 返回步骤1 → 开始新的识别任务
```

---

## 🐛 Bug修复

### 2️⃣ 文件输入框重置问题

修复了点击"重新开始"后，重新选择**相同文件**时无反应的bug。

**问题描述**：
- 重置状态后，文件输入框的`value`属性未被清空
- 导致选择相同文件时不触发`@change`事件
- 用户需要选择不同的文件才能继续

**修复内容**：
- ✅ 添加了文件输入框的`ref`引用
- ✅ 在重置时清空输入框的`value`属性
- ✅ 修复Vue2到Vue3的语法兼容（`$refs` → `ref.value`）

**影响文件**：
- `src/views/Dashboard.vue`
- `src/stores/detection.js`

---

## 🔧 参数优化

### 3️⃣ 默认检测参数调整

根据停车位检测场景的特点，优化了默认参数配置。

| 参数 | 旧默认值 | 新默认值 | 说明 |
|------|---------|---------|------|
| **置信度阈值** | 0.25 | **0.5** | 更严格的检测标准，减少误检 |
| **IOU阈值** | 0.45 | **0.6** | 更适合密集场景（如停车位） |
| 图片大小 | 640 | 640 | 保持不变 |
| 最大检测数 | 300 | 300 | 保持不变 |

**参数影响分析**：

📊 **置信度阈值提高 (0.25 → 0.5)**
- ✅ 减少假阳性（误检）
- ✅ 提高检测结果的可信度
- ⚠️ 可能会遗漏一些低置信度的真实目标

📊 **IOU阈值提高 (0.45 → 0.6)**
- ✅ 在密集场景下保留更多检测框
- ✅ 更适合停车位等紧密排列的场景
- ⚠️ 可能在某些情况下出现更多重叠框

**更新的文件**：
- `src/stores/detection.js` - 初始默认值
- `src/views/Dashboard.vue` - 重置时的默认值
- `README.md` - 文档说明

---

## 📚 文档更新

- ✅ `CHANGELOG.md` - 添加v1.1.0更新记录
- ✅ `README.md` - 更新最近更新部分和参数说明
- ✅ `PROJECT_SUMMARY.md` - 更新版本信息
- ✅ `package.json` - 版本号更新为1.1.0
- ✅ `RELEASE_NOTES_v1.1.0.md` - 本发布说明

---

## 🔄 升级指南

### 从v1.0.0升级到v1.1.0

1. **拉取最新代码**
   ```bash
   git pull origin main
   ```

2. **更新前端依赖**（如有变化）
   ```bash
   npm install
   ```

3. **重启开发服务器**
   ```bash
   npm run dev
   ```

4. **清除浏览器缓存**（可选）
   - 按 `Ctrl + F5` 强制刷新

### 注意事项

⚠️ **参数变更影响**
- 如果您之前保存了特定的检测参数配置，本次更新不会影响它们
- 新用户或使用"重新开始"功能时，将使用新的默认参数
- 您可以在步骤3中随时调整参数

⚠️ **无数据丢失**
- 所有历史记录和云端文件完全保留
- "重新开始"功能只清空当前界面状态

---

## 🧪 测试建议

建议在升级后进行以下测试：

1. ✅ **测试重新开始功能**
   - 完成一次识别流程
   - 点击"重新开始"按钮
   - 确认是否正常返回步骤1

2. ✅ **测试文件重新选择**
   - 点击重新开始后
   - 尝试上传相同的模型文件
   - 确认是否能正常触发上传

3. ✅ **测试新默认参数**
   - 使用默认参数进行识别
   - 对比与之前的检测结果差异

---

## 🎯 未来规划

下一个版本（v1.2.0）计划：
- [ ] 批量文件处理
- [ ] 模型性能对比
- [ ] 检测结果导出（JSON/CSV）
- [ ] 自定义类别过滤

---

## 🙏 致谢

感谢所有用户的反馈和建议，您的意见帮助我们不断改进产品！

如有任何问题或建议，欢迎：
- 📧 提交Issue
- 💬 参与讨论
- ⭐ Star本项目

---

**Happy Detecting! 🚀**
