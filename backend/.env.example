# 后端配置
PORT=8000
HOST=0.0.0.0

# Supabase配置（必须，用于上传文件到存储桶和保存记录到数据库）
# 在Supabase控制台 Settings > API 中获取
SUPABASE_URL=https://your-project.supabase.co
# 请使用 service_role key（服务角色密钥）而非anon key
SUPABASE_KEY=your_service_role_key
