# 快速配置默认权重

让每个新用户都自带 `best.pt` 权重的快速配置指南。

## 🎯 目标

配置完成后，所有新注册的用户无需上传模型，即可直接使用默认的 `best.pt` 权重进行检测。

## ⚡ 快速步骤

### 1️⃣ 上传权重到 Supabase

**最简单的方式 - 使用自动化脚本：**

```bash
# 激活虚拟环境
.\.venv\Scripts\Activate.ps1

# 运行上传脚本
cd backend
python upload_default_weight.py ..\best.pt
```

脚本会自动：
- ✅ 检查配置是否正确
- ✅ 上传文件到 Supabase
- ✅ 显示详细进度
- ✅ 处理覆盖确认

### 2️⃣ 配置环境变量

在 `.env` 文件中确认有以下配置（通常已自动配置）：

```bash
DEFAULT_MODEL_PATH=default/best.pt
DEFAULT_MODEL_NAME=默认停车位检测模型
```

### 3️⃣ 重启后端服务

```bash
.\restart_backend.bat
```

## ✅ 验证

### 方法1：查看日志

使用一个没有上传权重的新账户进行检测，后端日志应显示：

```
INFO: 用户没有自定义权重，尝试使用默认权重
INFO: 使用默认权重: 默认停车位检测模型
```

### 方法2：测试检测

1. 注册一个新账户（不要上传任何权重）
2. 直接进行图片或视频检测
3. 应该能成功返回检测结果

## 📊 工作原理

```
新用户注册 → 发起检测请求 → 系统自动使用默认权重 → 返回检测结果
```

用户可以随时上传自己的权重来替换默认权重。

## 🔧 手动上传（如果脚本失败）

如果自动化脚本遇到问题，可以手动上传：

1. 登录 [Supabase 控制台](https://supabase.com)
2. 进入 Storage → `model-weights` 存储桶
3. 创建 `default` 文件夹
4. 上传 `best.pt` 文件

最终路径应该是：`model-weights/default/best.pt`

## ❓ 常见问题

**Q: 上传脚本提示 "SUPABASE_URL 或 SUPABASE_KEY 未配置"**

A: 确保 `.env` 或 `backend/.env` 文件中配置了这些变量。

**Q: 用户上传自己的权重后会怎样？**

A: 系统会优先使用用户的自定义权重，不再使用默认权重。

**Q: 如何更新默认权重？**

A: 重新运行上传脚本，选择覆盖即可：
```bash
python upload_default_weight.py ..\new_best.pt
```

## 📚 完整文档

详细配置说明请参考：[DEFAULT_WEIGHT_SETUP.md](./DEFAULT_WEIGHT_SETUP.md)

---

配置完成后，所有新用户都可以直接体验检测功能了！🎉
